import { PriorityTaskPipe } from './priority-task.pipe';

describe('PriorityTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
